[project]
name = "ai-automation-service"
version = "0.1.0"
description = "Vivid Static Lab automation tools: DocForge generator, client app stamper, and the static Business Hub site."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
